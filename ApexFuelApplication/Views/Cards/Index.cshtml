@*@attribute [Authorize]*@
@model GetCardsByCompanyResponseModel


@(Html.DevExtreme().DataGrid<ApexFuelApplication.Models.CardsByCompany>().DataSource(Model.Result)
    .AllowColumnResizing(false)
    )
    .RemoteOperations(true)
    .Columns(columns => {

        columns.AddFor(m => m.rcount);

        columns.AddFor(m => m.code);

        columns.AddFor(m => m.fuelname);
    })
)